1.CSS box model deep understanding:-
The CSS Box Model is a conceptual framework that describes how the elements of a webpage are structured and rendered. It consists of four components: content, padding, border, and margin. 
Each of these components plays a vital role in the overall appearance and spacing of an element.
The CSS Box Model defines how elements are sized and positioned by assigning a box in the DOM tree that determines an element's dimensions and its position relative to other elements.
Content: The area where text or other content is displayed. This is the innermost part of the box where the actual content, such as text or images, is displayed. The width and height of 
this box can be controlled using the width and height properties.
Padding: Space between the content and the element's border. Padding is the space between the content and the border. It creates an inner cushion around the content, ensuring that the 
content does not touch the border directly. Padding can be set using the padding property, and it can have different values for each side (top, right, bottom, and left).
Border: A frame that wraps around the padding and content. The border wraps around the padding and content. It can be styled using properties like border-width, border-style, 
and border-color. The border can be set individually for each side or uniformly for all sides.
Margin: Space between the element's border and neighboring elements. The margin is the outermost layer of the box, creating space between the element and its neighboring elements. 
Margins are set using the margin property and can also have different values for each side.

2.Display modes: block, inline, inline-block:-
1. Using Display Block
This is the default property for <div> elements. It places them vertically, one after another. You can adjust the height and width of a block-level element. Used to display an element as a block element.
example:-
#group1 {
       background: teal;
       display: block;
}
#group2 {
       background: cyan;
       display: block;
}
#group3 {
       background: green;
       display: block;
}

2. Using Inline Display
Use this property to display an element inline. It doesn’t start a new line and respects the content flow. Used to display an element as an inline element.
example:-
#group1 {
       background: teal;
       display: inline;
}
#group2 {
       background: cyan;
       display: inline;
}
#group3 {
       background: green;
       display: inline;
}

3. Using Display Inline-block
Combining characteristics of both block and inline, this value allows elements to flow inline while still having block-level properties. It’s useful for creating responsive layouts.
Display an element as an inline-level block container.
example:-
#group1 
{
        background: teal;
        display: inline-block;
} 
#group2 {
        background: cyan;
        display: inline-block; 
} 
#group3 {
        background: green;
        display: inline-block;
}


3.Positioning: relative, absolute, sticky, fixed:-
1. Relative Positioning
An element with relative positioning stays in the normal flow, but you can move it relative to its original position using offsets (top, right, bottom, left).
The element still takes up its original space in the layout.
Useful for making small adjustments or setting a reference point for absolutely positioned child elements.
Key use case: Often used as the "positioning context" for absolutely positioned children.

2.Absolute Positioning
With absolute positioning, the element is completely removed from the normal document flow and positioned relative to its nearest positioned ancestor (anything that has a position other than static).
If no positioned ancestor exists, the element is positioned relative to the initial containing block (usually the element).
Doesn’t occupy space in the normal flow (other elements ignore it).
Perfect for overlays, popups, or elements that need to "float" inside a container.

3.Sticky Positioning
Sticky positioning is a hybrid between relative and fixed. It behaves like relative until the element reaches a defined threshold (e.g., top: 0), and then it "sticks" in place like a 
fixed element—but only within its parent container.
Moves normally while scrolling, until hitting the threshold.
Unlike fixed, it only sticks within its parent container—once the container scrolls out of view, the sticky element goes with it.
Great for table headers, section headings, or scroll-based UI designs.

4.Fixed Positioning
A fixed element is like an absolute element, but its reference point is always the viewport (the browser window). That means even when the page scrolls, the element stays pinned to its position.
Stays visible while the user scrolls.
Doesn’t move with page content.
Commonly used for sticky headers, floating buttons, or persistent navigation bars.

4.Flexbox alignment algorithms:-
A component can specify the layout of its children using the Flexbox algorithm. Flexbox is designed to provide a consistent layout on different screen sizes.
Flexbox is a one-dimensional layout system that allows for flexible and responsive layout of flex items. It is based on the Flexible Box Layout Module.
Flex Container: The parent element that contains the flex items.
Flex Items: The children elements within the flex container.
Main Axis: The primary axis along which flex items are laid out.
Cross Axis: The perpendicular axis to the main axis.
Flex Direction: The direction in which flex items are laid out (row, column, row-reverse, column-reverse).
Flex Wrap: Whether flex items wrap to a new line or not.

5.Grid layout system fundamentals:-
CSS grid layout introduces a two-dimensional grid system to CSS. Grids can be used to lay out major page areas or small user interface elements. This guide introduces the CSS grid layout and 
the terminology that is part of the CSS grid layout specification. The features shown in this overview will then be explained in greater detail in the other guides in this series.
A grid is a set of intersecting horizontal and vertical lines defining rows and columns. Elements can be placed onto the grid within these column and row lines.
The foundation of effective layout structuring lies in a deep comprehension of grid fundamentals. A well-designed grid is the backbone of a visually appealing and harmonious composition, providing a 
sense of balance and order. To achieve this, it is vital to understand the principles of grid alignment and grid ratios.

Grid alignment refers to the way elements are positioned within the grid, ensuring that they are properly aligned and spaced. This can be achieved through the use of columns, rows, and 
modules, which provide a clear structure for placing elements. Effective grid alignment helps to create a sense of cohesion and unity, making it easier for the viewer to navigate the layout.

6.Z-index stacking context rules:-
Stacking contexts can be contained in other stacking contexts, and they can together create a hierarchy of stacking contexts.
The root element of a document is a stacking context which, in most cases, contains nested stacking contexts, many of which will contain additional stacking contexts. 
Within each stacking context, child elements are stacked according to the same rules explained in Using z-index. Importantly, the z-index values of its child stacking contexts only have 
meaning within its parent's stacking context. Stacking contexts are treated atomically as a single unit in the parent stacking context.
example:-
<!DOCTYPE html>
<html>

<head>
    <title>Stacking with z-index</title>

    <style>
        .box {
            box-sizing: border-box;
            font-family: Arial;
            color: #eee;
            width: 125px;
            height: 125px;
            text-align: center;
        }

        .blue,
        .green {
            position: absolute;
        }

        .red {
            background: red;
            z-index: 100;
        }

        .green {
            background: green;
            top: 40px;
            left: 40px;
            z-index: 3;
        }

        .blue {
            background: blue;
            top: 80px;
            left: 80px;
            z-index: 2;
        }
    </style>
</head>

<body>
    <div class="box green">Positioned</div>
    <div class="box blue">Positioned</div>
    <div class="box red">Non-positioned</div>
</body>

</html>

7.Build hospital dashboard layout using Flexbox:-
//index.html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Hospital Dashboard</title>
<link rel="stylesheet" href="style.css">
</head>

<body>

<div class="container">

  <aside class="sidebar">
    <h2>Hospital</h2>
    <ul>
      <li>Dashboard</li>
      <li>Patients List</li>
      <li>Doctors List</li>
      <li>Appointments List</li>
    </ul>
  </aside>

  <div class="main">

    <header class="header">
      <h2>Dashboard</h2>
      <button>Logout</button>
    </header>

    <section class="cards">
      <div class="card">Total Patients: 100</div>
      <div class="card">Doctors: 25</div>
      <div class="card">Appointments: 50</div>
    </section>

    <section class="patients">
      <h3>Patient List</h3>
      <ul id="patientList"></ul>
    </section>

  </div>

</div>

<script src="app.js"></script>
</body>
</html>

//app.js
const patients = [
  "Gayatri wasulkar",
  "Chaitali kene",
  "Parth shinde",
  "Vaishnavi biradar"
];

const patientList = document.getElementById("patientList");

patients.forEach(name => {
  const li = document.createElement("li");
  li.textContent = name;
  patientList.appendChild(li);
});

//style.css
*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:Arial;
}

.container{
display:flex;
height:100vh;
}

.sidebar{
width:220px;
background:brown;
color:white;
padding:20px;
}

.sidebar ul{
list-style:none;
margin-top:20px;
}

.sidebar li{
margin:15px 0;
cursor:pointer;
}

.main{
flex:1;
display:flex;
flex-direction:column;
background:#f4f6f8;
}

.header{
display:flex;
justify-content:space-between;
align-items:center;
padding:15px 20px;
background:white;
border-bottom:1px solid #ddd;
}

.cards{
display:flex;
gap:20px;
padding:20px;
}

.card{
flex:1;
background:white;
padding:20px;
border-radius:8px;
box-shadow:0 2px 5px rgba(0,0,0,0.1);
}

.patients{
padding:20px;
}

.patients ul{
margin-top:10px;
}

.patients li{
background:white;
padding:10px;
margin-bottom:10px;
border-radius:5px;
}

8.Recreate 3 complex UI layouts:-
1.Gmail-Style Layout
//index.html
<div class="gmail">

  <header class="topbar">Gmail</header>

  <div class="content">

    <aside class="sidebar">
      <p>Inbox</p>
      <p>Starred</p>
      <p>Sent</p>
      <p>Draft</p>
    </aside>

    <section class="email-list">
      <div class="email">Email Box 1</div>
      <div class="email">Email Box 2</div>
      <div class="email">Email Box 3</div>
    </section>

    <section class="preview">
      <h3>Email Preview</h3>
      <p>Message content here...</p>
    </section>

  </div>

</div>
//style.css
.gmail{
display:flex;
flex-direction:column;
height:100vh;
}

.topbar{
background:purple;
color:white;
padding:15px;
}

.content{
display:flex;
flex:1;
}

.sidebar{
width:200px;
background:#f1f1f1;
padding:20px;
}

.email-list{
flex:1;
padding:20px;
}

.preview{
width:300px;
background:#fafafa;
padding:20px;
}

2.Netflix-Style Movie Layout
//index.html
<header class="nav">
  Netflix
</header>

<div class="movies">

  <h3>Trending</h3>
  <div class="row">
    <div class="movie"></div>
    <div class="movie"></div>
    <div class="movie"></div>
  </div>

  <h3>Popular</h3>
  <div class="row">
    <div class="movie"></div>
    <div class="movie"></div>
    <div class="movie"></div>
  </div>
<p>Show more Details to Login and Sign Up Now </p>
 <button onclick="Login"> Login</button>
 <button onclick="Sign Up"> Sign Up</button>
</div>
//style.css
.nav{
background:black;
color:white;
padding:15px;
}

.movies{
padding:20px;
}

.row{
display:flex;
gap:20px;
margin-bottom:20px;
}

.movie{
width:150px;
height:200px;
background:brown;
}
.button{
  width:200px;
  height:150px;
  padding:10px;
}

3.Chat Application Layout
//index.html
<header class="nav">
  <h3>WhatsApp</h3>
</header>
<div class="chat-app">

  <aside class="users">
    <p>Gayatri</p>
    <p>Omkar</p>
    <p>Shantanu</p>
  </aside>

  <div class="chat">

    <div class="messages">
      <div class="msg">Hello</div>
      <div class="msg">How are you?</div>
    </div>

    <div class="input">
      <input type="text" placeholder="Type message">
      <button>Send</button>
    </div>

  </div>

</div>
//style.css
.nav{
background:rgb(144, 238, 144);
color:black;
padding:1px;
}
.chat-app{
display:flex;
height:100vh;
}

.users{
width:200px;
background:#eee;
padding:20px;
}

.chat{
flex:1;
display:flex;
flex-direction:column;
}

.messages{
flex:1;
padding:20px;
}

.input{
display:flex;
padding:10px;
gap:10px;
}

9.Fix 5 CSS bugs related to positioning:-
//index.html
<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>CSS Positioning Bugs Fix</title>
<link rel="stylesheet" href="style.css">
</head>

<body>

<header>Fixed Header</header>

<div class="content">

<section class="section">
<h3>1 Absolute Position Fix:</h3>

<div class="parent">
<div class="child">Child Box</div>
</div>

</section>


<section class="section">

<h3>2 Fixed Header Fix:</h3>

<p>
This content is visible because margin-top is added
to avoid overlapping with the fixed header.
</p>

</section>

<section class="section">

<h3>3 z-index Fix:</h3>

<div class="box-container">

<div class="box1">Box 1</div>
<div class="box2">Box 2</div>

</div>

</section>

<section class="section">

<h3>4 Sticky Navigation:</h3>

<div class="sticky-nav">
Sticky Navigation Bar
</div>

<p style="height:300px;">
Scroll to see sticky behavior.
</p>

</section>

<section class="section">

<h3>5 Center Element Fix:</h3>

<div class="center-parent">
<div class="center-box">
Centered Box
</div>
</div>

</section>

</div>

</body>
</html>

//style.css
body{
font-family:Arial;
margin:0;
padding:20px;
}

.section{
margin-bottom:60px;
border:1px solid #ccc;
padding:20px;
}

.parent{
height:120px;
background:#eee;
position:relative;
}

.child{
position:absolute;
top:10px;
left:10px;
background:blue;
color:white;
padding:10px;
}

header{
position:fixed;
top:0;
left:0;
width:100%;
height:50px;
background:brown;
color:white;
display:flex;
align-items:center;
padding-left:20px;
}

.content{
margin-top:70px;
}

.box-container{
position:relative;
height:120px;
}

.box1{
position:absolute;
top:20px;
left:20px;
background:blue;
color:white;
padding:20px;
z-index:10;
}

.box2{
position:absolute;
top:40px;
left:40px;
background:orange;
padding:20px;
}

.sticky-nav{
position:sticky;
top:0;
background:green;
color:white;
padding:10px;
}

.center-parent{
position:relative;
height:200px;
background:#ddd;
}

.center-box{
position:absolute;
top:50%;
left:50%;
transform:translate(-50%,-50%);
background:purple;
color:white;
padding:20px;
}
